.DS_Store
Thumbs.db
node_modules/
.vscode/
.idea/
*.log

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# preview/build artifacts (not part of the deployed site)
.playwright-mcp/
site/
.worker-assets/
_site/

# local-only reference material (NOT deployed — kept off GH Pages)
assets/anavanzin_site_asset_pack/
assets/anavanzin_site_asset_pack.zip
FOTOSSSS/
.superpowers/

# scratch: images for Claude to evaluate (never deploy)
assets/_eval/

# agent session working notes — tracked files are deployed (see CLAUDE.md
# "Deployment"), so these must never re-enter the tree
.agents/
.ecc/
*_HANDOFF.md
ORIGINAL_REQUEST.md
